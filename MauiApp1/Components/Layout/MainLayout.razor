@* 继承自LayoutComponentBase，表示这是一个布局组件 *@
@inherits LayoutComponentBase

@* 页面主容器，包含侧边栏和主内容区 *@
<div class="page">
    @* 侧边栏容器，包含导航菜单 *@
    <div class="sidebar">
        <NavMenu /> @* 导航菜单组件 *@
    </div>

    @* 主内容区 *@
    <main>
        @* 顶部工具栏，包含一个外部链接 *@
        <div class="top-row px-4">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div>

        @* 主要内容区域，渲染当前页面内容 *@
        <article class="content px-4">
            @Body @* 占位符，渲染当前路由对应的页面内容 *@
        </article>
    </main>
</div>
